# Python dependencies for LoRA training
# Install with: pip install -r requirements-training.txt
#
# IMPORTANT: For RTX 5090 (sm_120) support, install PyTorch with CUDA 12.8:
# pip uninstall torch torchvision torchaudio
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# For other GPUs, the standard torch installation should work:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

transformers>=4.35.0
peft>=0.6.0
datasets>=2.14.0
torch>=2.5.0  # Minimum version for better GPU support
accelerate>=0.24.0
bitsandbytes>=0.41.0  # Optional, for 8-bit training

