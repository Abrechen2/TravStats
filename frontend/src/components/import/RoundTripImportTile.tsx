/**
 * Stub — full implementation lands in Task 9 (round-trip XLSX/CSV/JSON re-import).
 * This placeholder exists solely so ImportSection.tsx compiles until Task 9 replaces it.
 */
export function RoundTripImportTile(): JSX.Element {
  return (
    <div className="import-tile import-tile--stub">
      <p style={{ color: "var(--text-muted)", fontStyle: "italic" }}>
        TODO: Round-Trip re-import — implemented in Task 9.
      </p>
    </div>
  );
}
